<h1>This is the index page for Songs</h1>

<%# @songs is being called from the controller. Where here the data is shown. %>
<% @songs.each do |song| %>
    <li> <a href="/songs/<%= song.slug %>"> <%= song.name %> </a></li>
    <% end %>